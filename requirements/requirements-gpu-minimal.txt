# Minimal GPU-enabled requirements for SermonAudio AI Audio Processor
# This file installs essential GPU acceleration with minimal overhead
# Install with: uv pip install -r requirements/requirements-gpu-minimal.txt

# GPU-enabled PyTorch (CUDA 12.1 support)
--extra-index-url https://download.pytorch.org/whl/cu121

# Core dependencies from base requirements
-r requirements.txt

# Essential GPU-specific overrides
# These replace the CPU versions from requirements.txt
torch==2.1.1+cu121
torchaudio==2.1.1+cu121
torchvision==0.16.1+cu121

# Essential GPU monitoring
nvidia-ml-py3>=7.352.0
pynvml>=11.4.1

# Memory management for GPU workloads
accelerate>=0.21.0

# Note: This minimal configuration provides:
# - GPU-accelerated PyTorch for audio processing
# - GPU monitoring capabilities
# - Memory management for large models
# - Full compatibility with existing CPU fallback code
#
# Requirements:
# - NVIDIA GPU with 4GB+ memory
# - CUDA 12.1 compatible driver
# - Automatically falls back to CPU if GPU unavailable
#
# For enhanced GPU features, use requirements-gpu.txt
# For maximum acceleration, use requirements-gpu-full.txt
