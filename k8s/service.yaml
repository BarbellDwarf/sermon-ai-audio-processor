# k8s/service.yaml
apiVersion: v1
kind: Service
metadata:
  name: sermon-processor-service
  namespace: sermon-processor
spec:
  selector:
    app: sermon-processor
  ports:
  - name: web
    port: 80
    targetPort: 8501
  - name: api
    port: 8000
    targetPort: 8000
  type: ClusterIP